/**
 * @description 라우터 경로
 * @example
 * import { ROUTES } from "src/routes";
 * console.log(ROUTES.MAIN.root); // "/"
 * @author 이호연
 */
export const ROUTES = {
   /** 메인 화면 */
   MAIN: { root: "/" },
};
